------------------------------------------------------
Thursday 07 May 2026,
------------------------------------------------------
WORK done:
Authentication:
POST /api/auth/register/         OK
POST /api/auth/login/            OK
POST /api/auth/token/refresh/    OK
GET  /api/auth/me/               OK

Public Directory:
GET /api/directory/              OK
GET /api/directory/<slug>/       OK

Merchant:
GET   /api/merchant/profile/     OK
PATCH /api/merchant/profile/     OK
------------------------------------------------------
TESTED:
registering any user (merchant):
POST http://127.0.0.1:8000/api/auth/register/
{
    "username": "merchant2",
    "email": "merchant2@gmail.com",
    "password": "strongpassword123@",
    "is_merchant": true
}

login merchant2 
POST /api/auth/login/
{
    "username": "merchant2",
    "password": "strongpassword123@"
}
receive tokens:
{
    "refresh": "...",
    "access": "..."
}

authenticated test (see tests/auth_me.py)
set Authorization: Bearer <access_token>
then tested: 
GET /api/auth/me/
GET /api/merchant/profile/
------------------------------------------------------
TO DO next:
- merchant image upload endpoint
- category system
- verification document upload 
- leads analytics
- payment integration
------------------------------------------------------


------------------------------------------------------
Thursday 08 May 2026,
------------------------------------------------------
WORK done:
Categories:
GET /api/categories/

Merchant Images:
POST /api/merchant/images/

Verification Upload:
PATCH /api/merchant/verify/

Lead Recording:
POST /api/directory/1/contact-click/

Merchant Analytics:
GET /api/merchant/stats/
------------------------------------------------------
TO DO next:
test categories, merchant images, verification upload,
lead recording and merchant analytics endpoints.

payment integration: Subscription + Payment Webhook System
------------------------------------------------------


------------------------------------------------------
Thursday 10 May 2026,
------------------------------------------------------
exposed:
- Categories
- Leads 
- Merchants verification 
- Subscription status
through admin first before building custom dashboards

improved merchant admin in regard to images upload

TESTED endpoints:
Categories:
GET /api/categories/                        OK

Merchant Images:
POST /api/merchant/images/                  OK

Verification Upload:
PATCH /api/merchant/verify/                 OK

Lead Recording:
POST /api/directory/1/contact-click/        OK

Merchant Analytics:
GET /api/merchant/stats/                    OK
------------------------------------------------------
TO DO next
payment integration: Subscription + Payment Webhook System
------------------------------------------------------


------------------------------------------------------
Thursday 11 May 2026,
------------------------------------------------------
PayChangu is better for our Malawi-focus marketplace 
because:
- local mobile money support
- local banking support 
- local trust 
- MWK support 
- better regional adoption

PAYMENT ARCHITECTURE FLOW:
Merchant
   ↓
Initiate Subscription Payment
   ↓
PayChangu Checkout URL
   ↓
Merchant Pays
   ↓
PayChangu Webhook
   ↓
Your Backend Verifies Transaction
   ↓
Subscription Activated
   ↓
Merchant Becomes Public

WHAT TO BE BUILD:
1 - subscription model: tracks payment lifecycle 
2 - payment initiation endpoint 
POST /api/payments/initialize/ : creates PayChangu payment
3 - Webhook endpoint 
POST /api/payments/webhook/ : receives payment confirmation
4 - transaction verification 
the backend verifies payment with PayChangu before activating
subscription.
5 - Automatic merchant activation
profile.is_active_subscription = True 

SECURITY PRINCIPLE:
as we NEVER trust frontend payment success.
we ALWAYS verify:
- transaction
- amount
- currency
- status 
from backend [that will be about the webhook i will build]

I created PAYCHANGU_WEBHOOK_SECRET and set TEST WEBHOOK URL in
PayChangu as PayChangu cannot reach: localhost or 127.0.0.1 directly
I hence set TEST WEBHOOK URL: https://your-ngrok-url.ngrok-free.app/api/payments/webhook/
For that i had to install NGROK for handling webhook notifications 
(like "payment successful")back to my local machine, and NGROK acts 
as a secure tunnel.
For some reason ngrok fails on my machine, so i will try pinggy.link
: ssh -p 443 -R0:localhost:8000 a.pinggy.io
then: yes
and paste the provided urls in PayChangu Test Webhook url 
ex: [https://npmkp-103-106-200-58.run.pinggy-free.link
https://npmkp-103-106-200-58.run.pinggy-free.link/api/payments/webhook/
pinggy.io WORKS

created subscription/payment models 
later in apps/payment/services.py set the real frontend url for:
callback_url and return_url
done: /api/payments/initialize/


DONE:
payment initialization:
/api/payments/initialize/            OK when response returned, 
                                    the most important part is 
                                    the checkout_url in the response
                                    (it is the hosted payment page 
                                    generated by PayChangu)

Resume so far:
- API keys work 
- request payload is valid
- authentication works 
- merchant subscription creation works 
- PayChangu accepts the integration 
Hence i have officially a working payment initialization system 
------------------------------------------------------


payments are initialized
BUT merchant subscriptions are not yet activated automatically
after successful payment

------------------------------------------------------
Thursday 12 May 2026,
------------------------------------------------------
implementation of:
- webhook receiver
- transaction verification 
- automatic subscription 

added paychangu settings (core/settings.py)
created transaction verification service (apps/payments/services.py)
created webhook view (apps/payments/views.py)
added webhook url (apps/payments/urls.py)

test:
api/payments/webhook/

flow:
first user starts payment by:
   frontend calls:
   POST /api/payments/initialize/

   the backend:
      creates a local subscription object
      generates tx_ref
      contacts PayChangu 
      receives checkout URL (MOST IMPORTANT)
         ex: 
            {...
               "checkout_url": "...",
               "tx_ref": "8d9ad83f-d791-402d-b81d-23012311630c"
            ...
            }

the frontend then redirects user to checkout_url 
ex: 
https://test-checkout.paychangu.com/3545399530
this page is hosted by PayChangu 
   then user:
      enters payment info
      completes payment

then PayChangu sends webhook automatically (MOST IMPORTANT PART)
after payment succeeds:
PayChangu servers automatically call:
   POST /api/payments/webhook/
   to our backend, the frontend does not do this.
   PayChangu does it server-to-server automatically
   [as we should never trust the frontend ex: Never trust
   frontend payment success alone
   instead we only trust webhook verification from PayChangu
   that is the importance of webhook]

our backend verifies payment 
   verifies webhook secret 
   verifies transaction with PayChangu API 
   checks actual payment status 
   activates subscription 

subscription activated 
   our backend updates:
      Subscription:
         status to "paid"
      MerchantProfile:
         is_active_subscription to True
   and now merchant appears publicly in marketplace
------------------------------------------------------

problem i'm facing, after succesfully proceeding
the payment on PayChangu sandbox, it don't see any 
request being made to my backend implemented
payments webhook.


------------------------------------------------------
Thursday 13 May 2026,
------------------------------------------------------
updated apps/payments/models.py PayChanguWebhookView to 
align with PayChangu Webhook documentation

updated apps/payments/services.py (added webhook_url to payload)

after testing with curl the webhook endpoint:
apple@APPLEs-MacBook-Pro ~ % curl -X POST https://skqgb-103-106-200-58.run.pinggy-free.link/api/payments/webhook/ \
-H "Content-Type: application/json" \
-d '{
"event": "charge.success",
"data": {
"tx_ref": "47cf138b-c000-44e7-8647-36d034cd0f92"
}
}'

i got:
{"error":"Invalid signature"}%

which means:
If PayChangu sends a real webhook request correctly,
my endpoint SHOULD receive it. as every else work fine:
i.e pinggy tunnel works, Django endpoint is publicly reachable,
/api/payments/webhook/ is accessible, external POST requests
reach Django, webhook route works, CSRF is not blocking, paychangu
could theoretically reach my backend 
So now the issue narrows down to:
- either PayChangu is not sending webhooks
- or my signature verification implementation is incorrect
- or webhook secret/signature header format is wrong

the only thing failing now is: Invalid signature
technically it means that my webhook view is now validating signatures. 
the manual curl request did not contain the PayChangu signature header 
so Django correctly rejects it. 

PROBLEM NARROWED: IT IS SIGNATURE VERIFICATION

According to PayChangu Webhooks Documentation, PayChangu sends a 
header named: 
Signature 
and it is:
HMAC SHA256(raw_request_body, webhook_secret)

so my current implementation is probably computing the signature 
incorrectly or reading the wrong header format
------------------------------------------------------

the problem of PayChangu not hitting the /api/payments/webhook endpoint 
persist.


------------------------------------------------------
Thursday 15 May 2026,
------------------------------------------------------
updated PayChanguWebhookView to handle the specific header 
and the correct hashing algorithm

i tested everything: and i am confident that: 
my tunnel pinggy.io is working, my Django server is reachable 
and my webhook view logic is firing. 
Since the curl works but the actual PayChangu sandbox doesn't 
send anything to my logs, i have narrowed it down to two 
possibilities.
- signature secret key 
- PayChangu is not hitting my webhook url

decided to test webhook manually on PayChangu Dashboard:
and Signature Verification is successful. my backend and 
PayChangu are now officially talking to each other securely.

now the final issue i am seeing in the logs is a "failed" verification response:
'status': 'failed', 'message': 'Payment transaction not created.'

This is happening because of how PayChangu Sandbox handles the 
"Test Webhook" button versus a "Real" transaction.

Why the Verification is failing
In My PayChanguWebhookView, after i verify the signature, 
i call verify_paychangu_transaction(tx_ref).
      When using the "Test Webhook" button: PayChangu sends 
      a fake payload. Since this transaction doesn't actually 
      exist in their database, when my backend tries to 
      verify it via their API, PayChangu returns status: 
      failed.
      When doing a "Real" Sandbox payment: The transaction 
      does exist, and the verification should return success.

the FIX: NOT verifying "Test" events
so updated my PayChanguWebhookView accordingly

problem SOLVED
------------------------------------------------------




------------------------------------------------------
Thursday 16 May 2026,
------------------------------------------------------
now that i solved the issue of not receiving the webhook
response from PayChangu. that means:
- PayChangu successfully sent the webhook
- my Django backend received it
- Signature verification passed
- my webhook logic executed
- Subscription was updated
- Merchant subscription status updated
- Pinggy tunnel worked
- PayChangu dashboard webhook configuration worked
my payment architecture is now functional.

PAYMENT ARCHITECTURE FLOW IMPLEMENTED AND TESTED SO FAR:
Merchant                               
   ↓
Initiate Subscription Payment          OK
   ↓
PayChangu Checkout URL                 OK
   ↓
Merchant Pays                          OK
   ↓
PayChangu Webhook                      OK
   ↓
Your Backend Verifies Transaction      OK
   ↓
Subscription Activated                 OK
   ↓
Merchant Becomes Public                OK


Improvement to do:
1. Protect premium endpoints with subscription permissions
2. Automatically expire subscriptions
3. Improve webhook idempotency & safety
4. Add payment event logs
5. Improve frontend payment flow
6. Prepare for production deployment


implemented protection of premium endpoints with subscription permissions
      Right now:
         merchants can pay
         subscription activates
      BUT nothing prevents unsubscribed merchants from using premium features.
      
      We want: Only merchants with active subscriptions can access protected endpoints.

      created apps/payments/permissions.py and implemented HasActiveSubscription and added to my merchants views(views that require payment first)

implemented automatic subscription expiration
      Right now:
         subscriptions become active 
      BUT they never automatically expire, So merchants could remain active forever after one payment.

      We want: to Deactivate merchants when subscription expires.
      
      we will do that by Cron Job / scheduler hence no celery complexity needed and scalable enough and it is used in real systems

      created apps/payments/management/

      testing expire_subscriptions flow
         run: python manage.py expire_subscriptions
         if no expired subscription it will print: Total expired subscriptions: 0

         manually change a paid subscription on admin to yesterday so that it is expired 
         and run: python manage.py expire_subscriptions
         we should get: 
            Expired subscription: ...
            Total expired subscriptions: 1

            and check on admin if the particular subscription get updated to status = expired hence the particular merchant profile should show is_active_subscription = False

implemented improvement webhook idempotency & safety
implemented payment event logs
      why it matters:
         In production:
            payments fail
            webhooks fail
            signatures fail
            users complain
            gateways retry
      So We NEED permanent records.

      Professional systems ALWAYS store:
         raw payload
         signature
         event type
         processing result
         timestamps

      This becomes:
         debugging tool
         audit trail
         fraud analysis
         support evidence

   created a PaymentEvent model in apps/payments/models.py
   modified apps/payments/views.py

implementated Production-Grade Subscription Renewal Logic hence updated subscription renewal logic
added calculate_subscription_expiry function in apps/payments/services.py 
and updated webhook logic for subscription renewal logic apps/payments/views.py

used PaymentEvent event ID, reference and transaction ID to solve possibility of idempotency with actual logic
------------------------------------------------------



------------------------------------------------------
Thursday 16 May 2026,
------------------------------------------------------
implemented correctly so far
- webhook signature verification
- secure HMAC comparison
- JSON validation
- tx_ref validation
- idempotency protection
- event logging
- PayChangu transaction verification
- atomic DB transaction
- subscription activation
- renewal expiry logic
- proper logging
- failure handling
- duplicate webhook protection

the current system is already:
- safe
- maintainable
- scalable enough for MVP
- understandable
- professionally structured
------------------------------------------------------

TO DO:
PostgreSQL migration
Environment variables cleanup
Dockerize backend
Docker Compose
Static/media handling
API documentation
Production settings split later

------------------------------------------------------
integrating PostgreSQL
   i install PostgreSQL using brew: brew install postgresql
   start PostgreSQL command: brew services start postgresql
   check if it is running: psql postgres

   brew list | grep postgres
postgresql@18 is installed

STARTING POSTGRESQL 
to check status:(running or not)
   brew services list

to run it: brew services start postgresql@18
then check again: 
   brew services list
i should see: 
   postgresql@18 started

TESTING POSTGRESQL 
run:
   psql postgres
if successful, i should enter PostgreSQL shell:
   postgres=#
exist with:
   \q

CREATING THE POSTGRESQL DATABASE 
run:
   createdb masika_wathu_db
then verifying:
   psql postgres
inside shell:
   \l 
i should see:
   masika_wathu_db 
exist:
   \q

INSTALLING PYTHON DRIVER
inside the virtual Django virtual environment:
   pip install psycopg2-binary django-environ
then:
   pip freeze > requirements.txt

------------------------------------------------------
Thursday 20 May 2026,
------------------------------------------------------
INTEGRATING POSTGRESQL:
------------------------
PostgreSQL installed 
PostgreSQL service running 
Database created 
psycopg2 installed 
django-environ installed 

i can connect Django to PostgreSQL now
added database variables to .env 
updated settings.py 
replaced:
   DATABASES = {
    'default': env.db(
        default=f"sqlite:///{BASE_DIR / 'db.sqlite3'}"
    )
   }
with:
   DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": env("DB_NAME"),
        "USER": env("DB_USER"),
        "PASSWORD": env("DB_PASSWORD"),
        "HOST": env("DB_HOST"),
        "PORT": env("DB_PORT"),
    }
   }

then run migrations to test PostgreSQL connection:
i should see migrations applying normally

i successfully migrated to PostgreSQL 
now i can:
- see tables
- inspect rows 
- query data 
- see merchants 
- see subscriptions 
- see payment events 
   how to?
      run to be inside 
         PostgreSQL: psql masika_wathu_db
      show all tables: 
         \dt
      i will see tables like:
         payments_subscription
         payments_paymentevent
         merchants_merchantprofile
         users_customuser
         ...
      to view subscription rows:
         SELECT * FROM payments_subscription;
      etc...

      to exit:
         \q 

   OR JUST USE EASIER VISUAL INTERFACE PROVIDER
      pgAdmin 4
      or 
      TablePlus


to stop PostgreSQL when Done working:
   brew services stop postgresql@18
verify:
   brew services list

to start PostgreSQL Next day: 
   brew services start postgresql@18
verify:
   brew services list

how to check if Django is using PostgreSQL:
   python manage.py dbshell
if it opens PostgreSQL shell:
   psql (18.x)
then Django is connected to PostgreSQL successfully 


DOCKERIZING THE BACKEND: portable production environment 
------------------------


it is important so that backend works on any machine and 
make deployment easier, no "works on my machine" problems 

installed docker at: https://www.docker.com/products/docker-desktop/
Due to some issue with incompatibility with my mac version i 
install Docker version 20.10.13, build a224086

------------------------------------------------------
Friday 22 May 2026,
------------------------------------------------------
created Dockerfile : touch Dockerfile
created .dockerignore
created docker-compose.yml 
changed in .env DB_HOST=localhost to DB_HOST=db
building containers: docker-compose up --build

to remove all broken postgres volum: (for avoiding the corrupted/incompatible volume remains)
   docker-compose down -v

for migrating inside docker:
   docker-compose exec web python manage.py migrate

create superuser:
   docker-compose exec web python manage.py createsuperuser


can now go directly to: 
   http://localhost:8000
admin:
   http://localhost:8000/admin


Right now 
| Component     | Running Where                   |
| ------------- | ------------------------------- |
| Django        | Docker container                |
| PostgreSQL    | Docker container                |
| Code          | Your Mac mounted into container |
| Database data | Docker volume                   |
| Networking    | Docker internal network         |


important command:
stop containers:
   docker-compose down
start containers again later:
   docker-compose up
rebuild after major dependency changes:
   docker-compose up --build
see running containers:
   docker ps
open PostgreSQL shell inside container:
   docker-compose exec db psql -U postgres -d masika_wathu_db
   then:
   \dt 
   see tables 
   for ex: SELECT * FROM payments_subscription;
   exit:
   \q 


The Docker PostgreSQL is now DIFFERENT from the local PostgreSQL installation.
I now effectively have:
- Local Mac PostgreSQL
- Docker PostgreSQL
For backend development:
focus on Docker PostgreSQL now.

------------------------------------------------------

I will use drf-spectacular GitHub for the API documentation 
because it is production-grade and much better maintained than 
old Swagger packages.

Openning swagger docs:
http://localhost:8000/api/docs/
and:
http://localhost:8000/api/redoc/

Because i have two environment: local machine and docker environment 
local machine:
   python environment 
   (myenv)
Docker environment:
   isolated backend container 
   when i install new packages locally:
      ex: pip install something 
   Docker Does not automatically get them:
   i must:
      pip freeze > requirements.txt
      docker-compose build
   so Docker installs them too 

Problem facing right now:
= persistent Docker PostgreSQL storage
= proper docker-compose volumes
= automatic migrations
= production-ready startup command
= .env separation
= Docker optimization

WHAT I WILL FIX
1. Persistent PostgreSQL storage
the DB survives container restart/removal.

2. Proper docker-compose volumes
Better code syncing + DB persistence.

3. Automatic migrations
No need to manually migrate every time.

4. Production-ready startup command
Backend boots correctly and consistently.

5. .env separation
Secrets/config isolated properly.

6. Docker optimization
Cleaner, smaller, faster containers.


Now i have:
   persistent database storage: db survives restart, db survives container build, real persistence 
   automatic migrations: migrations auto-run at startup 
   .env separation: no secrets inside code anymore
------------------------------------------------------




NEXT STEPS
- Static/media handling
   production-ready static files 
- Gunicorn
   replace Django dev server
- Nginx reverse proxy
   Reverse proxy
- Production settings split 
   dev.py / prod.py 
- Health checks 
- Celery + Redis 
   background jobs 
- CI/CD pipeline
- Automated testing 
- https Deployment










POSSIBILITY TO CHEK IT OUT:
Search/filtering

IMPORTANT FOR PRODUCTION:
should automate the expire_subscriptions later on in production to maybe run each midnight (Linux Con)
make sure PayChangu urls and secret keys are production ready
should delete the db.sqlite3 database when fully confident PostgreSQL works
should make late cross check of docker before deployment